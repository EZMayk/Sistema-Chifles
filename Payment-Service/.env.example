# ===========================================
# Payment-Service - Variables de Entorno
# ===========================================

# Puerto del servicio
PORT=3002
NODE_ENV=development

# ===========================================
# Base de Datos PostgreSQL
# ===========================================
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=admin
DATABASE_PASSWORD=admin123
DATABASE_NAME=sistema-chifles

# ===========================================
# HMAC Security
# ===========================================
HMAC_SECRET=payment_hmac_secret_key_2024

# ===========================================
# Stripe Configuration
# ===========================================
# Obtén tus claves en: https://dashboard.stripe.com/apikeys
# Usa las claves de TEST para desarrollo (empiezan con pk_test_ y sk_test_)

# Publishable Key (se usa en frontend) - empieza con pk_test_ o pk_live_
STRIPE_PUBLISHABLE_KEY=pk_test_tu_clave_publica_aqui

# Secret Key (se usa en backend) - empieza con sk_test_ o sk_live_
STRIPE_SECRET_KEY=sk_test_tu_clave_secreta_aqui

# Webhook Secret - se obtiene al crear un webhook endpoint en Stripe Dashboard
# O usando Stripe CLI: stripe listen --forward-to localhost:3002/webhooks/stripe
STRIPE_WEBHOOK_SECRET=whsec_tu_webhook_secret_aqui

# ===========================================
# Servicios Externos
# ===========================================
# URL del API Rest
API_REST_URL=http://localhost:3000

# URL del WebSocket Server
WS_URL=http://localhost:8080/notify
WS_SECRET=super_secret_key_123
